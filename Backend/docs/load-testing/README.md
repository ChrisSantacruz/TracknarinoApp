# Backend Load Testing Artifacts

This directory stores generated staging load summaries and device-lab bundles. Reports must come from real staging inputs only.

Expected artifact types:

- `load-summary-*.json`
- `device-lab-bundle-*.json`
- readiness snapshots captured before and after stress windows

Do not commit secrets, raw JWTs, or private GPS traces unless the repository policy explicitly allows encrypted evidence storage.
